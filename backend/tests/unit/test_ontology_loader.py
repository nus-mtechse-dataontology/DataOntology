"""
Ontology loader/resolver tests

Goal in Sprint 1:
- Ontology loads from a JSON file (or dict) and supports lookups used in grounding.

Start with these tests:
1) load_ontology() returns a dict with expected top-level keys
2) Missing/invalid ontology file -> clear failure (exception or error)
3) Lookup helpers (if you add them):
   - entity exists
   - field exists under entity/source
"""
import pytest


def test_placeholder_ontology_loader():
    # TODO Sprint 1: implement after ontology_v1.json is finalized
    assert True
